<section class="hero">
  <div class="container">
    <div class="hero-content">
      <h1>Save Money by Archiving Your Service Bills</h1>
      <p>
        We help you find discounts and reduce costs in your service bills. Our easy-to-use platform stores and analyzes your bills to identify savings opportunities.
      </p>
      <div class="hero-buttons">
        <a href="#pricing" class="btn btn-primary">View Pricing</a>
        <a href="#services" class="btn">Learn More</a>
      </div>
    </div>
    <div class="hero-image">
      <img src="/images/hero-image.svg" alt="Bill savings illustration" />
    </div>
  </div>
</section>

<style>
  .hero {
    padding: 6rem 0;
    background: linear-gradient(to right, var(--color-background), var(--color-background-alt));
  }
  
  .hero .container {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
  }
  
  .hero-content {
    flex: 1;
  }
  
  .hero-content h1 {
    font-size: 3rem;
    line-height: 1.2;
    margin-bottom: 1.5rem;
    color: var(--color-text);
  }
  
  .hero-content p {
    font-size: 1.2rem;
    color: var(--color-text-light);
    margin-bottom: 2rem;
    max-width: 600px;
  }
  
  .hero-buttons {
    display: flex;
    gap: 1rem;
  }
  
  .hero-image {
    flex: 1;
    display: flex;
    justify-content: flex-end;
  }
  
  .hero-image img {
    max-width: 100%;
    height: auto;
  }
  
  @media (max-width: 768px) {
    .hero .container {
      flex-direction: column;
      text-align: center;
    }
    
    .hero-content h1 {
      font-size: 2.5rem;
    }
    
    .hero-buttons {
      justify-content: center;
    }
    
    .hero-image {
      justify-content: center;
      margin-top: 2rem;
    }
  }
</style> 